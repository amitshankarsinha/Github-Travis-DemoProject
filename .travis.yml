language: java
install: true
dist: trusty
jdk:
  - oraclejdk8
before_script:
  - chmod +x gradlew
script:
  - ./gradlew build --stacktrace
  - pwd
  - cd build/libs
  - ls

deploy:
  provider: releases
  #api_key:
   #  secure: travis encrypt -r amitshankarsinha/Github-Travis-DemoProject
  api_key: $GIT_TOKEN_NEW
  #user: "amitshankarsinha"
  #password: "semester#123"
  file: "build/libs/githubTravisIntregration.jar"
  skip_cleanup: true
  on:
    tags: false
    branch: master
