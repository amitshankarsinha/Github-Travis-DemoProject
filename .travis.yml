language: java
install: true
dist: trusty
jdk:
  - oraclejdk8
before_script:
  - chmod +x gradlew
script:
  - ./gradlew build --stacktrace
  - ./gradlew jar --stacktrace

#deploy:
 # provider: releases
  #user: ""
  #password: ""
  #file: "build/libs/githubTravisIntregration.jar"
  #skip_cleanup: true
  #on:
   # tags: false
    #branch: master
